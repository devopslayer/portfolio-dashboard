import "./Header.css";

const Header = () => (
  <nav className="navbar navbar-dark bg-dark px-3">
    <span className="navbar-brand mb-0 h1">Portfolio Dashboard</span>
  </nav>
);

export default Header;
